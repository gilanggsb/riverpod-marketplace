class ProductRequestParams {
  final int? limit;
  final int? offset;
  final String? searchKey;
  final int? categoryId;

  ProductRequestParams({this.limit, this.offset, this.searchKey, this.categoryId});
}
